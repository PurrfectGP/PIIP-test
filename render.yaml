services:
  - type: web
    name: felix-poly-sin-lab
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: GEMINI_API_KEY
        sync: false  # You must set this manually in the Render dashboard
      - key: GEMINI_MODEL
        value: gemini-2.5-pro-preview-05-06
